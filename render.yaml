services:
  - type: web
    name: petoy-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python petoy.py
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: TELEGRAM_BOT_TOKEN
        sync: false
